from crew import run_resume_tailor_crew
from resume_tailoring.tools import extract_resume_text, extract_job_info




if __name__ == "__main__":
    run_resume_tailor_crew()

    # Example usage (optional, remove if not needed)
    # resume_text = extract_resume_text("path/to/resume.pdf")
    # job_info = extract_job_info("path/to/job_description.txt")
